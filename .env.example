# Required for Google one-click sign-in (injected into manifest at build time).
# Create a "Chrome extension" OAuth client in Google Cloud Console using your extension ID.
VITE_GOOGLE_CLIENT_ID=your-extension-client-id.apps.googleusercontent.com
