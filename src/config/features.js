/** Product mode: local DOM extraction only — no OpenAI / chat. */
export const AI_CHAT_ENABLED = false;

/** Register selections with syncle-services when signed in (for dashboard history). */
export const CLOUD_SYNC_ENABLED = true;
