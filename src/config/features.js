/** Feature flags (runtime AI mode is chrome.storage.sync aiEnabled). */
export const AI_CHAT_ENABLED = true;

/** Register selections with syncle-services when AI mode is on and signed in. */
export const CLOUD_SYNC_ENABLED = true;
